Python

适用于Windows/Linux/macos等
使用少量代码（少1/5左右代码量相较于其他语言）

一般用于 云计算/Web/操作系统等

我所需要学习的：
	Python3所有常用语法
	面向对象的编程实现
	模块化编程
	游戏编程
	计算机仿真等

（https://www.bilibili.com/video/av4050443/?p=11）（使用Python3.6.5）
我将使用Python IDLE以及PyCharm来进行Python代码的基础知识学习以及调试代码(上面是我学习Python时看的视频教程)


其中我们使用 # 进行单行注释使用
             ''' 进行多行注释使用

Python语言中代码 对齐 就相当于C语言中的 { } （这个在Python程序编写时非常重要！！！！！！！！）

（1）使用变量前，必须对其先赋值
（2）变量名可以包括字母、数字、下划线，但是变量名不可以字母开头
（3）编写代码时给变量一个专业写的名词

转义字符： \
原始字符： r +加字符串
多行字符串：'''     ''' 或 “““    ”””

数值类型  整型
         布尔型
	 浮点型
	 e记法

True = 1
False = 0 (一般不用于计算，千万别使用账，否则程序最终会出现不可遇料bug)

整数  int()
浮点数  float()
字符串的 str()

幂运算                         **
正负号                      +X    -X
算术操作符                *  /  //  +  -
比较操作符            <<=    >>=    ==    !=
逻辑运算符         not         and            or

条件表达式（三元操作符）：
        x if 条件 else y    条件成立为x,反之为y
	
assert()  (条件断言)	
      用于调试程序，出错会抛出AssertionError并自动崩溃
      
            
while 条件：
    循环体
    
for循环
  for 目标 in 表达式：
      循环体
      

创建普通列表
创建混合列表
创建空列表

向列表中添加或删除元素
  append()   1个元素在最后追加
  extend()   多个元素在最后追加
  insert(位置,  )  放入任意位置
  remove（ ） 删除列表中的元素
  del        列表中的值
  pop()      去除列表中最后一个值并返回刚删掉的值
  
  
列表切片是列表中极其重要的工具
  一般会以拷贝的形式出现   原始数据不会改变
  
dir(list)  可以列出列表中所有指令
  count()  查询其出现的次数
  index(  [,  ,  ,])   后面两个表示查找范围      出现的位置
  reversc()    将元素直接倒序
  sort(func,key,reversc=True)   从大到小   什么都不加则从小到大       前两个默认即可
  
  
做一个飞机的流程：
    加载背景音乐
    播放背景音乐
    while True:
        if 用户是否点击了关闭按钮：
	    退出程序
        小飞机诞生
        小飞机移动一个位置
        屏幕刷新
	if 用户鼠标产生移动：
	    我方飞机中心位置=用户鼠标位置
	    屏幕刷新
	if 我方飞机与小飞机发生肢体冲突：
	    我方亡播放撞击音乐
	    修改我方飞机图案
	    打印游戏结束
	    停止背景音乐 最好淡出




